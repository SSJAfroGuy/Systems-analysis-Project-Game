if(storyText = 4 && (instance_number(objEnemyProjectile) == 0))
{
	storyText += 1;
	Textbox_Creation("Praise to those adventurers who have reached my final note. You have learned all I can teach about these dreaded catacombs. But be warned, nothing here is the same twice over. These rooms are the only constant. Expect everything and anything. Most importantly: if it moves towards you, kill it! Good luck ~ Tori", 
0.5, 300, 300);
}